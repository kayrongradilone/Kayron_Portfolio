import Loader from 'react-loaders';
import Layout from '../Layout';
import './index.scss';

const Contact = () => {
    return (
        <>
            <Layout/>
            <Loader type='pacman'/>
        </>
    )
}

export default Contact